# File System Checking

This project performs a file system check on an xv6 file system image.
It performs checks according to a list of rules (omitted here).

To run this program:
1. Run xv6 with QEMU, and make any desired changes to the filesystem.
2. Run `fcheck` with `fs.img` as input.
